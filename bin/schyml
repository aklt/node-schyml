#!/usr/bin/env node

var argv = process.argv

argv.shift()
argv.shift()

var usage = "Usage: yaml-mate <faker|knex> filename.yml"

argv.push(require('../lib/helpers').cb)

require('../lib/main').apply(null, argv)

